<UserControl x:Class="AspitAktivitet.GUI.AdminPlans"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AspitAktivitet.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
             
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="9*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Label Content="Mulige"/>
            <ListView x:Name="lwUnassigned"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <Button x:Name="cmbAssigned" Content=">" Margin=" 0 200 0 0"/>
            <Button x:Name="cmbUnassigned" Content="\>"/>
        </StackPanel>
        <StackPanel Grid.Column="2">
            <Label Content="Tildelt"/>
            <ListView x:Name="lwAssigned"/>
        </StackPanel>    
            
    </Grid>
</UserControl>
