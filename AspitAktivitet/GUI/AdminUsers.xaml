<UserControl x:Class="AspitAktivitet.GUI.AdminUsers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AspitAktivitet.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Label Content="Opret en bruger" />
            <Label Content="Navn:"/>
            <TextBox Name="txtName"/>
            <CheckBox Content="Admin"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="cmdUpdate" Grid.Column="2" Content="Opdater"/>
                <Button x:Name="cmdCreate" Grid.Column="3" Content="Opret"/>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <Label Content="Liste af bruger"/>
            <ListView Height="250">
                <!--Insert content from DB-->
            </ListView>
            <Button x:Name="cmdDelete" Content="Slet"/>
            
        </StackPanel>
    </Grid>
</UserControl>
